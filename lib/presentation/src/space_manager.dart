import 'package:flutter/material.dart';

class BuildSpace {
  static SizedBox horizontal(double width) => SizedBox(width: width);
  static SizedBox virtical(double height) => SizedBox(height: height);
}
